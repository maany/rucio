---
phase: 11-simplify-votests-workflow
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/simplify_votests.yml
  - .github/workflows/simple-autotest.yml
autonomous: true
requirements: [SUIT-07]
must_haves:
  truths:
    - "A new .github/workflows/simplify_votests.yml exists and is valid YAML"
    - "The new workflow triggers on pull_request, push, workflow_dispatch, and schedule (cron 0 3 * * *)"
    - "The new workflow runs a policy matrix of atlas AND belleii via the plugin (python -m pytest --suite=votest --policy=<X>)"
    - "Each policy leg produces a distinct junit file, artifact, and check report (votest-<policy>)"
    - "simple-autotest.yml no longer contains a votest matrix entry (atlas votest removed)"
  artifacts:
    - path: ".github/workflows/simplify_votests.yml"
      provides: "Dedicated two-policy votest workflow driven by the ruciopytest plugin"
      contains: "--suite=votest"
    - path: ".github/workflows/simple-autotest.yml"
      provides: "PR autotest workflow with votest leg removed"
  key_links:
    - from: ".github/workflows/simplify_votests.yml"
      to: "runtime_images.yml"
      via: "uses: ./.github/workflows/runtime_images.yml"
      pattern: "runtime_images.yml"
    - from: ".github/workflows/simplify_votests.yml"
      to: "ruciopytest plugin"
      via: "python -m pytest --suite=votest --policy=${{ matrix.policy }}"
      pattern: "--policy="
---

<objective>
Create a new dedicated CI workflow `.github/workflows/simplify_votests.yml` that runs the
votest (policy) suite for BOTH `atlas` and `belleii` via the ruciopytest plugin, mirroring
upstream legacy `vo_tests.yml` layout/cadence but using the modern plugin path. Simultaneously
remove the atlas-only votest leg from `simple-autotest.yml`, since votest now lives ONLY in the
new dedicated workflow (mirroring upstream's separate-workflow layout).

Purpose: Restore the belleii votest CI coverage that per-PR CI currently omits (it runs atlas
only), closing the votest-belleii gap from the v1.1 milestone audit (SUIT-07).
Output: New `simplify_votests.yml`; edited `simple-autotest.yml` with votest removed.
</objective>

<execution_context>
@/home/maany/.claude/get-shit-done/workflows/execute-plan.md
@/home/maany/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-simplify-votests-workflow/11-CONTEXT.md

# The template to copy: simple-autotest.yml's votest leg + shared steps.
@.github/workflows/simple-autotest.yml
# Legacy cadence/naming reference.
@.github/workflows/vo_tests.yml

<interfaces>
<!-- Executor should NOT redesign. Copy the shared step structure from simple-autotest.yml
     verbatim, dropping only the client-provisioning and multi_vo-specific pieces. -->

simple-autotest.yml votest matrix entry to DELETE (lines ~53-57):
```yaml
          - suite: votest
            leg: votest
            python: "3.9"
            rdbms: postgres14
            policy: atlas
```
NOTE: the "Run tests" step, upload-artifact steps, and "Publish test report" step in
simple-autotest.yml are GENERIC (keyed off ${{ matrix.leg }} / ${{ matrix.python }} /
${{ matrix.policy }}). They are SHARED by all legs — do NOT delete them. Removing the votest
matrix `include` entry above is sufficient to remove the votest leg AND its report from
simple-autotest.yml (the report step only renders for legs that exist in the matrix).

Shared steps to COPY into the new workflow (from simple-autotest.yml), in order:
  - Checkout code (actions/checkout@93cb6efe...)
  - Set up Python (actions/setup-python@e797f83b..., python-version: 3.9, cache: pip,
    cache-dependency-path: requirements/requirements.dev.txt)
  - Install host plugin deps (pip install -c requirements/requirements.dev.txt pytest pytest-xdist pyyaml)
  - Login to GitHub Container Registry (docker/login-action@c94ce9fb..., continue-on-error: true)
  - Set up Docker Buildx (if needs.runtime_images.outputs.build_locally == 'true')
  - Compute image metadata (if build_locally; repo + pynodot outputs)
  - Build runtime image locally (if build_locally; docker/build-push-action@471d1dc4...,
    PYTHON=3.9, tags = needs.runtime_images.outputs.py39_image)
  - Run tests (see task action)
  - Upload test results (always, name test-results-${{ matrix.leg }}-py${{ matrix.python }})
  - Upload container logs (failure, name container-logs-${{ matrix.leg }}-py${{ matrix.python }})
  - Upload host logs (failure, include-hidden-files: true, name host-logs-${{ matrix.leg }}-py${{ matrix.python }})
  - Publish test report (always, mikepenz/action-junit-report@v6,
    check_name: Tests - ${{ matrix.leg }} (py${{ matrix.python }}), fail_on_failure: true)

Run tests step (from simple-autotest.yml, votest-relevant form):
```yaml
      - name: Run tests
        env:
          RUCIO_TEST_IMAGE: ${{ needs.runtime_images.outputs.py39_image }}
          RDBMS: ${{ matrix.rdbms }}
          PYTHON: ${{ matrix.python }}
          POLICY: ${{ matrix.policy }}
        run: |
          set -o pipefail
          mkdir -p test-results
          python -m pytest \
            --suite=votest \
            --policy=${{ matrix.policy }} \
            --junitxml=test-results/${{ matrix.leg }}-py${{ matrix.python }}.xml \
            -r fExX \
            --log-level=DEBUG \
            -v --tb=short \
            tests/ 2>&1 | tee test-results/${{ matrix.leg }}-py${{ matrix.python }}.pytest.log
```
Because python is fixed at 3.9, the runtime image is always py39_image — DROP the
`matrix.python == '3.10' && ... || ...` ternary and use py39_image directly everywhere.

Legacy vo_tests.yml (upstream, with pull_request/push active) cadence to match:
  on: pull_request, push, workflow_dispatch, schedule (cron '0 3 * * *')
  concurrency: group ${{ github.workflow }}-${{ github.ref }}, cancel-in-progress: true
  strategy.fail-fast: false
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create simplify_votests.yml (two-policy plugin-driven votest workflow)</name>
  <files>.github/workflows/simplify_votests.yml</files>
  <action>
Create a new workflow file that is a near-copy of `simple-autotest.yml`'s votest leg plus the
shared runtime-images/report scaffolding, specialized to a two-policy votest matrix.

Concretely:
- `name: VO-specific tests (ruciopytest)`
- Triggers (match legacy vo_tests.yml with pull_request/push ACTIVE — this is the real upstream
  cadence per 11-CONTEXT decision, which OVERRIDES ROADMAP criterion #1's "not per-PR" wording):
  ```yaml
  on:
    pull_request:
    push:
    workflow_dispatch:
    schedule:
      - cron: '0 3 * * *'
  ```
- `concurrency:` group `${{ github.workflow }}-${{ github.ref }}`, `cancel-in-progress: true`.
- `jobs.runtime_images:` `uses: ./.github/workflows/runtime_images.yml` with
  `permissions: { contents: read, packages: write }` (copy verbatim from simple-autotest.yml).
- `jobs.test:` `needs: [runtime_images]`, `runs-on: ubuntu-latest`,
  `permissions: { checks: write, contents: read }`.
- Matrix (STATIC, hardcoded — NOT the dynamic votest_helper.py matrix):
  ```yaml
  strategy:
    fail-fast: false
    matrix:
      include:
        - policy: atlas
          leg: votest-atlas
          python: "3.9"
          rdbms: postgres14
        - policy: belleii
          leg: votest-belleii
          python: "3.9"
          rdbms: postgres14
  ```
- Steps: copy the shared steps listed in <interfaces> IN ORDER (Checkout → Set up Python →
  Install host plugin deps → GHCR login → Buildx → Compute image metadata → Build runtime image
  locally → Run tests → Upload test results → Upload container logs → Upload host logs → Publish
  test report). Keep the exact pinned action SHAs from simple-autotest.yml.
- Because python is fixed at 3.9, use `needs.runtime_images.outputs.py39_image` directly for
  RUCIO_TEST_IMAGE and the build `tags:` — drop the 3.10 ternary. In "Compute image metadata"
  you may keep `repo`/`pynodot` (pynodot will be `39`) since the build cache-from ref uses it.
- The "Run tests" step MUST use the votest form shown in <interfaces>: `--suite=votest
  --policy=${{ matrix.policy }}`, `POLICY: ${{ matrix.policy }}` in env, junit/log paths keyed
  off `${{ matrix.leg }}` so atlas and belleii never collide (votest-atlas-py3.9.xml /
  votest-belleii-py3.9.xml).
- DO NOT copy the client-provisioning steps (Install full rucio on host, Provision rucio server
  for client, Run client tests in-container fallback) or the `RUCIO_MULTI_VO_LEG` env — they are
  not relevant to votest.
- The Publish test report step keeps `fail_on_failure: true` (both legs hard-fail) and
  `check_name: Tests - ${{ matrix.leg }} (py${{ matrix.python }})` → yields "Tests - votest-atlas
  (py3.9)" / "Tests - votest-belleii (py3.9)".
- Do NOT install/config belleii policy package here — the plugin's in-container `[policy]` rewrite
  (Phase 07-01) is expected to handle policy selection identically for both policies. Any
  belleii-specific provisioning that turns out to be needed is discovered/fixed in Plan 11-02
  (the live workflow_dispatch verification). Leave the run step identical for both policies here.
  </action>
  <verify>
    <automated>python -c "import yaml,sys; d=yaml.safe_load(open('.github/workflows/simplify_votests.yml')); assert d, 'empty'; import re; s=open('.github/workflows/simplify_votests.yml').read(); assert '--suite=votest' in s and '--policy=' in s, 'missing plugin invocation'; assert 'belleii' in s and 'atlas' in s, 'missing a policy'; assert 'workflow_dispatch' in s and 'schedule' in s and 'pull_request' in s and 'push' in s, 'missing a trigger'; assert 'runtime_images.yml' in s, 'missing runtime_images'; print('OK')"</automated>
  </verify>
  <done>
`.github/workflows/simplify_votests.yml` exists, is valid YAML, has all four triggers
(pull_request/push/workflow_dispatch/schedule cron '0 3 * * *'), a static atlas+belleii matrix,
calls runtime_images.yml, and invokes `python -m pytest --suite=votest --policy=<X>` with
per-policy junit/artifact/report naming (votest-atlas / votest-belleii). fail-fast: false and
fail_on_failure: true both present.
  </done>
</task>

<task type="auto">
  <name>Task 2: Remove the votest leg from simple-autotest.yml</name>
  <files>.github/workflows/simple-autotest.yml</files>
  <action>
Delete the votest matrix `include` entry from `simple-autotest.yml` (the block shown in
<interfaces>: suite=votest / leg=votest / python 3.9 / rdbms postgres14 / policy: atlas,
around lines 53-57). This removes the atlas votest leg entirely.

Do NOT touch the generic shared steps (Run tests, Upload*, Publish test report) — they are shared
by the remaining legs (remote_dbs x2, multi_vo-tst, multi_vo-ts2, client). Removing the matrix
entry is sufficient to remove both the votest leg and its rendered report from this workflow.

Leave the `POLICY: ${{ matrix.policy }}` line in the shared "Run tests" env as-is — with no
votest leg in the matrix, `matrix.policy` is simply empty for the remaining legs (harmless, and
avoids churn). Do not remove it.

After the edit, the matrix must contain exactly 5 legs: remote_dbs (py3.9), remote_dbs (py3.10),
multi_vo-tst, multi_vo-ts2, client — no votest.
  </action>
  <verify>
    <automated>python -c "import yaml; d=yaml.safe_load(open('.github/workflows/simple-autotest.yml')); inc=d['jobs']['test']['strategy']['matrix']['include']; legs=[e.get('leg') for e in inc]; assert 'votest' not in legs and not any('votest' in (l or '') for l in legs), f'votest still present: {legs}'; assert len(inc)==5, f'expected 5 legs, got {len(inc)}: {legs}'; print('OK legs:', legs)"</automated>
  </verify>
  <done>
`simple-autotest.yml` matrix has 5 legs and no votest entry; the workflow remains valid YAML and
the shared steps are untouched.
  </done>
</task>

</tasks>

<verification>
- Both workflow files parse as valid YAML.
- `simplify_votests.yml` has the four triggers, static atlas+belleii matrix, plugin invocation,
  runtime_images call, and per-policy naming.
- `simple-autotest.yml` no longer has any votest leg (5 legs remain).
- `git diff --stat` shows exactly these two files changed.
</verification>

<success_criteria>
- New `.github/workflows/simplify_votests.yml` created, mirroring legacy vo_tests.yml cadence
  (pull_request + push + workflow_dispatch + schedule) and simple-autotest.yml's votest leg shape.
- Runs both atlas and belleii via `python -m pytest --suite=votest --policy=<X>`.
- votest removed from simple-autotest.yml (atlas votest matrix entry + its report gone).
- ROADMAP criterion #1 "not per-PR" is superseded by the 11-CONTEXT locked decision (match legacy
  exactly = per-PR + push + nightly); record this in the SUMMARY.
</success_criteria>

<output>
After completion, create `.planning/phases/11-simplify-votests-workflow/11-01-SUMMARY.md`.
Note in the SUMMARY that the per-PR cadence intentionally supersedes ROADMAP success criterion #1.
</output>
