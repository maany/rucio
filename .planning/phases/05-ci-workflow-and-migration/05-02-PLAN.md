---
phase: 05-ci-workflow-and-migration
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/lint.yml
autonomous: true
requirements:
  - CICD-04

must_haves:
  truths:
    - "lint.yml runs pre-commit hooks (ruff, header checks, formatting) on pull requests"
    - "lint.yml runs type annotation regression check comparing current branch to ancestor commit"
    - "lint.yml runs pyright type check comparing current branch to ancestor commit"
    - "lint.yml is a separate workflow file, not embedded in autotest.yml or simple-autotest.yml"
  artifacts:
    - path: ".github/workflows/lint.yml"
      provides: "Separate lint and type checking workflow"
      contains: "pre-commit run"
  key_links:
    - from: ".github/workflows/lint.yml"
      to: ".github/actions/checkout_ancestor_commit"
      via: "uses: ./.github/actions/checkout_ancestor_commit"
      pattern: "checkout_ancestor_commit"
    - from: ".github/workflows/lint.yml"
      to: "tools/count_missing_type_annotations_utils.sh"
      via: "source tools/count_missing_type_annotations_utils.sh"
      pattern: "count_missing_type_annotations"
---

<objective>
Create a new lint.yml workflow that consolidates pre-commit hooks, type annotation regression checks, and pyright type checking into a single workflow file, triggered on pull requests.

Purpose: CICD-04 -- type checking and syntax checking run in a separate lint/format workflow, not in simple-autotest.yml. This consolidates the currently disabled code-quality.yml and the lint jobs from the disabled autotest.yml into one active workflow.

Output: .github/workflows/lint.yml with 3 jobs (pre_commit, python_annotations, pyright).
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/05-ci-workflow-and-migration/05-CONTEXT.md
@.planning/phases/05-ci-workflow-and-migration/05-RESEARCH.md

@.github/workflows/code-quality.yml
@.github/workflows/autotest.yml

<interfaces>
<!-- From autotest.yml -- reference patterns for lint jobs: -->
<!-- pre_commit job: actions/checkout@v5, actions/setup-python@v6, pre-commit run --all-files -->
<!-- python_annotations job: uses checkout_ancestor_commit action, count_missing_type_annotations_utils.sh -->
<!-- python_pyright job: uses checkout_ancestor_commit action, tools/run_pyright.sh -->

<!-- From .github/actions/checkout_ancestor_commit/ -- composite action for ancestor comparison -->
<!-- Requires fetch-depth: 0 on checkout step -->
<!-- Input: paths_to_copy (multiline list of files to preserve across checkout) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create lint.yml workflow</name>
  <files>.github/workflows/lint.yml</files>
  <action>
Create a new `.github/workflows/lint.yml` file consolidating all static analysis checks.

**Workflow metadata:**
- name: "Lint and Type Checks"
- Triggers: `pull_request` (no push, no schedule)
- Concurrency: `group: ${{ github.workflow }}-${{ github.ref }}`, `cancel-in-progress: true`

**Job 1: pre_commit**
- name: "Pre-commit (ruff, headers, formatting)"
- runs-on: ubuntu-latest
- Steps (copy pattern from autotest.yml pre_commit job):
  1. `actions/checkout@93cb6efe18208431cddfb8368fd83d5badbf9bfd` with `fetch-depth: 0`
  2. `actions/setup-python@e797f83bcb11b83ae66e0230d6156d7c80228e7c` with python-version: "3.11"
  3. `pip install pre-commit`
  4. `pre-commit run --all-files --show-diff-on-failure`

**Job 2: python_annotations**
- Copy EXACTLY from autotest.yml's python_annotations job (lines 34-89)
- Same `if` condition: skip for release branches, schedule events, and tags
- name: "Check Python Type Annotations"
- Same env vars: UPDATED_PYTHON_TYPE_ANNOTATIONS_REPORT_FILE, INITIAL_PYTHON_TYPE_ANNOTATIONS_REPORT_FILE
- Same steps: count current, checkout ancestor, count initial, diff, compare
- Use updated action refs: `actions/checkout@93cb6efe18208431cddfb8368fd83d5badbf9bfd` (v5)
- Use `pip install ruff` (not flake8 -- autotest.yml already uses ruff for this)

**Job 3: python_pyright**
- Copy EXACTLY from code-quality.yml's python_pyright job (lines 90-131)
- Same `if` condition as python_annotations
- name: "Python type check (Pyright)"
- Same env vars: PYRIGHT_CURRENT_REPORT, PYRIGHT_ANCESTOR_REPORT
- Same steps: checkout, install deps (npm pyright, pip requirements), generate current report, checkout ancestor, generate ancestor report, compare
- Use updated action refs: `actions/checkout@93cb6efe18208431cddfb8368fd83d5badbf9bfd` (v5)

**Key details:**
- Use pinned commit hashes for all GitHub Actions (same as autotest.yml uses)
- The `checkout_ancestor_commit` action is a local composite action at `.github/actions/checkout_ancestor_commit/` -- reference as `uses: ./.github/actions/checkout_ancestor_commit`
- Do NOT modify code-quality.yml or autotest.yml (both stay as-is per user decision)
- All 3 jobs run independently in parallel (no `needs` between them)
  </action>
  <verify>
    <automated>cd /media/scratchdisk/maany/Projects/Projects/rucio/rucio && python -c "import yaml; data=yaml.safe_load(open('.github/workflows/lint.yml')); jobs=list(data.get('jobs',{}).keys()); print('Jobs:', jobs); assert 'pre_commit' in jobs, 'Missing pre_commit'; assert 'python_annotations' in jobs, 'Missing python_annotations'; assert 'python_pyright' in jobs or 'pyright' in jobs, 'Missing pyright'; print('All 3 jobs present')" && grep -q "pre-commit run" .github/workflows/lint.yml && echo "Has pre-commit" && grep -q "checkout_ancestor_commit" .github/workflows/lint.yml && echo "Has ancestor checkout" && grep -q "run_pyright" .github/workflows/lint.yml && echo "Has pyright"</automated>
  </verify>
  <done>lint.yml is valid YAML with 3 jobs (pre_commit, python_annotations, python_pyright), triggers on pull_request, uses checkout_ancestor_commit for regression checks, runs pre-commit with ruff</done>
</task>

<task type="auto">
  <name>Task 2: Validate lint.yml references exist</name>
  <files>.github/workflows/lint.yml</files>
  <action>
Verify that all referenced local actions and tools exist in the repo:
1. Check `.github/actions/checkout_ancestor_commit/action.yml` exists
2. Check `tools/count_missing_type_annotations_utils.sh` exists
3. Check `tools/run_pyright.sh` exists
4. Check `.pre-commit-config.yaml` exists (needed for pre-commit run)

If any are missing, add a comment in the relevant lint.yml step noting the dependency. Do NOT create missing files -- they should already exist in the repo from previous work.

Also verify the lint.yml doesn't accidentally reference autotest.yml patterns that won't work standalone (e.g., shared env vars, matrix references).
  </action>
  <verify>
    <automated>cd /media/scratchdisk/maany/Projects/Projects/rucio/rucio && test -f .github/actions/checkout_ancestor_commit/action.yml && echo "checkout_ancestor_commit exists" && test -f tools/count_missing_type_annotations_utils.sh && echo "annotations util exists" && test -f tools/run_pyright.sh && echo "pyright script exists" && test -f .pre-commit-config.yaml && echo "pre-commit config exists"</automated>
  </verify>
  <done>All 4 referenced local files exist: checkout_ancestor_commit action, annotations util script, pyright script, pre-commit config</done>
</task>

</tasks>

<verification>
1. `python -c "import yaml; yaml.safe_load(open('.github/workflows/lint.yml'))"` -- valid YAML
2. lint.yml has exactly 3 jobs: pre_commit, python_annotations, python_pyright
3. lint.yml triggers on pull_request
4. All local action/tool references resolve to existing files
5. No overlap with simple-autotest.yml (lint.yml has no test execution)
</verification>

<success_criteria>
- lint.yml exists with 3 independent jobs covering pre-commit, type annotation regression, and pyright
- Workflow triggers on pull_request events
- Uses established patterns from autotest.yml and code-quality.yml (not reinvented)
- All referenced local actions and tools exist in the repo
</success_criteria>

<output>
After completion, create `.planning/phases/05-ci-workflow-and-migration/05-02-SUMMARY.md`
</output>
